# clipboard-gpt
A simple python program that adds openai's gpt4 to your keyboard.
# How to use
 - Open the main file edit the openai.api_key
 - run pip install -r requirements.txt
 - open the file with python 
 - copy some text, press control + alt + g to run it into gpt
 - Wait a bit, your text will be copied to your keyboard
